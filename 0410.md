함수(Functions)의 정의와 반환(Return) 처리
코드의 재사용성과 모듈화를 위한 핵심 개념인 함수 정의(def)와 결과값 반환(return)의 메커니즘을 학습하였습니다.

입력값(Parameter/Argument)을 받아 정해진 로직을 수행하고, 처리된 결과를 호출부로 안전하게 전달하는 데이터 흐름을 이해하였습니다.

중복되는 코드를 최소화하고 기능별로 로직을 분리함으로써, 전체 프로그램의 가독성과 유지보수성을 크게 향상시키는 구조적 프로그래밍을 실습하였습니다.